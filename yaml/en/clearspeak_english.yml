# For details on the format see https:///speechruleengine.org/yaml.md

# Rule that fires if an expression is visually abstracted
# Example: collapsed sum
# %1: The abstracted element
collapsed:
  - $1: "collapsed"
  - %1

# Explicit speaking of font
# Example: bold alpha
# %1: The font
# %2: The expression
font:
  - %1
  - %2 (pause:short)

# A character in fraktur font. Fraktur is sometimes also called blackletter or gothic. In English it is called German rather then the font name. Most other locales do not need this rule.
# %1: The character in fraktur font
german-font:
  - $1: "German"
  - %1

# A character in bold fraktur font. Fraktur is sometimes also called blackletter or gothic. In English it is called German rather then the font name. Most other locales do not need this rule.
# %1: The character in bold fraktur font
german-font-bold:
  - $1: "bold German"
  - %1

# Speaking an open ended ellipsis in AndSoOn preference
# Example: a comma b comma and so on
# LaTeX Example: $a, b, \ldots$
ellipsis:
  - $1: "and so on"

# Speaking an intermediate ellipsis in AndSoOn preference
# Example: a comma b comma and so on up to n
# LaTeX Example: $a,b,\ldots n$
ellipsis-andsoon:
  - $1: "and so on up to"

# Interprets an vertical with subscript as evaluation of an antiderivative
# Example: F of x, evaluated at x equals 0
# LaTeX Example: $F(x)|_{x=0}$
# %1: Expression left of the vertical bar.
# %2: The subscript to the right of the vertical bar
vbar-evaluated:
  - %1 (pause:short)
  - $1: "evaluated at"
  - %2 (pause:short)

# Interprets an vertical with subscript as evaluation of an antiderivative
# Example: F of x, evaluated at infinity, minus the same expression evaluated at 0
# LaTeX Example: $F(x)|_{0}^\infty$
# %1: Expression left of the vertical bar.
# %2: The superscript to the right of the vertical bar
# %3: The subscript to the right of the vertical bar
vbar-evaluated-both:
  - %1 (pause:short)
  - $1: "evaluated at"
  - %2 (pause:short)
  - $2: "minus the same expression evaluated at"
  - %3 (pause:short)

# Vertical bar interpreted as such that
# Example: a such that b
# LaTeX Example: $a|b$
vbar-such-that:
  - $1: "such that"

# Vertical bar interpreted as divides symbol
# Example: a divides b
# LaTeX Example: $a|b$
vbar-divides:
  - $1: "divides"

# Vertical bar interpreted as divides symbol, regardless of context
# Example: a divides b
# LaTeX Example: $a|b$
vbar-always-divides:
  - $1: "divides"

# Vertical bar interpreted as given, e.g., in a set context
# Example: a given b
# LaTeX Example: $a|b$
vbar-given:
  - $1: "given"

# Set membership symbol not within a set expression. Interpreted as member of. This is the fallback rule.
# Example: a is a member of A
# LaTeX Example: $a\in A$
member:
  - $1: "is a member of"

# Set membership symbol not within a set expression. Interpreted as member of
# Example: a is a member of A
# LaTeX Example: $a\in A$
member-member:
  - $1: "is a member of"

# Set membership symbol not within a set expression. Interpreted as element of
# Example: a is an element of A
# LaTeX Example: $a\in A$
member-element:
  - $1: "is an element of"

# Set membership symbol not within a set expression. Interpreted as is in
# Example: a is in A
# LaTeX Example: $a\in A$
member-in:
  - $1: "is in"

# Set membership symbol not within a set expression. Interpreted as is belongs to
# Example: a belongs to A
# LaTeX Example: $a\in A$
member-belongs:
  - $1: "belongs to"

# Set membership symbol not within a set expression. Interpreted as not member of. Fallback rule
# Example: a is not a member of A
# LaTeX Example: $a\not\in A$
not-member:
  - $1: "is not a member of"

# Set membership symbol not within a set expression. Interpreted as not member of
# Example: a is not a member of A
# LaTeX Example: $a\not\in A$
not-member-member:
  - $1: "is not a member of"

# Set membership symbol not within a set expression. Interpreted as not element of
# Example: a is not an element of A
# LaTeX Example: $a\not\in A$
not-member-element:
  - $1: "is not an element of"

# Set membership symbol not within a set expression. Interpreted as not in
# Example: a is not in A
# LaTeX Example: $a\not\in A$
not-member-in:
  - $1: "is not in"

# Set membership symbol not within a set expression. Interpreted as not belong to
# Example: a does not belong to A
# LaTeX Example: $a\not\in A$
not-member-belongs:
  - $1: "does not belong to"

# Set membership symbol within a set expression read in short form. Fallback rule.
# Example: a in A
# LaTeX Example: $\{a\in A| ...\}$
set-member:
  - $1: "in"

# Set membership symbol within a set expression read in short form. Interpreted as member of
# Example: a member of A
# LaTeX Example: $\{a\in A| ...\}$
set-member-member:
  - $1: "member of"

# Set membership symbol within a set expression read in short form. Interpreted as element of
# Example: a element of A
# LaTeX Example: $a\in A$
set-member-element:
  - $1: "element of"

# Set membership symbol within a set expression read in short form. Interpreted as in
# Example: a in A
# LaTeX Example: $a\in A$
set-member-in:
  - $1: "in"

# Set membership symbol within a set expression read in short form. Interpreted as belonging to
# Example: a belonging to A
# LaTeX Example: $a\in A$
set-member-belongs:
  - $1: "belonging to"

# Set membership symbol inside a set expression read in short form. Interpreted as not member of. Fallback rule
# Example: a not member of A
# LaTeX Example: $a\not\in A$
set-not-member:
  - $1: "not in"

# Set membership symbol inside a set expression read in short form. Interpreted as not member of
# Example: a not member of A
# LaTeX Example: $a\not\in A$
set-not-member-member:
  - $1: "not a member of"

# Set membership symbol inside a set expression read in short form. Interpreted as not element of
# Example: a not element of A
# LaTeX Example: $a\not\in A$
set-not-member-element:
  - $1: "not an element of"

# Set membership symbol inside a set expression read in short form. Interpreted as not in
# Example: a not in A
# LaTeX Example: $a\not\in A$
set-not-member-in:
  - $1: "not in"

# Set membership symbol inside a set expression read in short form. Interpreted as not belongs to
# Example: a not belonging to A
# LaTeX Example: $a\not\in A$
set-not-member-belongs:
  - $1: "not belonging to"

# A leading article is introduced. Only used in locales where articles are used for fractions or functions
# %1: The expression translated without article.
article:
  - $1: "the"
  - %1

# Fallback rule for function application
# Example: f of x
# LaTeX Example: $f(x)$
# %1: The function
# %2: The argument
appl:
  - %1
  - $1: "of"
  - %2 (pause:short)

# Function application of a single letter function like f, g, h.
# Example: f of x
# LaTeX Example: $f(x)$
# %1: The function
# %2: The argument
appl-simple:
  - %1
  - $1: "of" (pause:short)
  - %2 (pause:short)

# Function application of a single letter function with argument being another function application
# Example: f of h of x
# LaTeX Example: $f(h(x))$
# %1: The function
# %2: The argument, which is a fenced function application
appl-simple-fenced:
  - %1
  - $1: "of" (pause:short)
  - %2 (pause:short)

# Forces function applications to be read as juxtapositions
# Example: f times x
# LaTeX Example: $f(x)$
# %1: The function
# %2: The argument
appl-times:
  - %1
  - $1: "times"
  - %2 (pause:short)

# Application of a known prefix function, e.g., a trigonometric function
# Example: sine x
# LaTeX Example: $\sin x$
# %1: Prefix function
# %2: The argument
function-prefix:
  - %1
  - %2

# Application of a known prefix function to a fenced but simple argument (e.g., xy is simple, x + y is not)
# Example: sine x y
# LaTeX Example: $\sin(xy)$
# %1: The prefix function
# %2: The fenced argument
function-prefix-simple-arg:
  - %1
  - %2

# Application of a prefix function with index or superscript
# Example: sine squared x
# LaTeX Example: $\sin^2 x$
# %1: The embellished prefix function
# %2: The argument
function-prefix-embell:
  - %1
  - %2 (pause:short)

# Application of a prefix functions to a non-simple argument.
# Example: the sine of a over b
# LaTeX Example: $\sine(\frac{a}{b})$
# %1: 
# %2: The 
function-prefix-fenced-or-frac-arg:
  - %1 (grammar:addArticle)
  - $1: "of"
  - %2 (pause:short)

# Function application of a named function with subscript
# LaTeX Example: $\min_a X$
# %1: The prefix function with subscript
# %2: The argument of the function
function-prefix-subscript:
  - %1 (grammar:addArticle)
  - $1: "of" (pause:short)
  - %2 (pause:short)

# Natural logarithm application in short speech.
# Example: ln x
# %1: The natural logarithm.
# %2: The argument
function-ln:
  - %1
  - %2

# Natural logarithm application with following pause.
# Example: ln x
# %1: The natural logarithm.
# %2: The argument
function-ln-pause:
  - %1
  - %2 (pause:short)

# Natural logarithm application in explicit speech.
# Example: ln of x
# %1: The natural logarithm.
# %2: The argument
function-ln-of:
  - %1
  - $1: "of"
  - %2 (pause:short)

# Function in a superscript with a complex argument
# Example: x raised to the sine of open paren y plus z close paren power
# LaTeX Example: $x^{\sin (y + z)}$
# %1: The prefix function
# %2: The function argument
function-prefix-as-exp:
  - %1
  - $1: "of" (pause:short)
  - %2 (pause:short)

# Function in a subscript with a complex argument
# Example: x sub the sine of open paren y plus z close paren
# LaTeX Example: $x_{\sin (y + z)}$
# %1: The prefix function
# %2: Its argument
function-prefix-subscript-as-exp:
  - %1
  - $1: "of" (pause:short)
  - %2 (pause:short)

# Hyperbolic function as prefix function
# Example: hyperbolic sine of x
# LaTeX Example: $\sinh x$
# %1: The hyperbolic function
# %2: Its argument
function-prefix-hyper:
  - %1
  - $1: "of"
  - %2 (pause:short)

# Inverse of a named function
# Example: the inverse sine of x
# LaTeX Example: $\sin^{-1} x$
# %1: The inverted function (e.g., sin^-1)
# %2: Its argument
function-prefix-inverse:
  - $1: "the inverse"
  - %1
  - $2: "of"
  - %2 (pause:short)

# Speaks an arcus function as inverse
# Example: the inverse sine of x
# LaTeX Example: $\arcsin x$
# %1: The arcus function
# %2: Its argument
appl-triginverse:
  - %1
  - $1: "of"
  - %2 (pause:short)

# Speaks an inverse trigonometric function as arcus
# Example: arc sine x
# LaTeX Example: $\sin^{-1} x$
# %1: The inverted function (e.g., sin^-1)
# %2: Its argument
function-prefix-arc-simple:
  - $1: "arc"
  - %1
  - %2 (pause:short)

# Speaks an inverted trigonometric function as arcus. Omits the parentheses on simple arguments.
# Example: arc sine x
# LaTeX Example: $\sin^{-1} (x)$
# %1: The inverted function (e.g., sin^-1)
# %2: The fenced argument with simple content
function-prefix-arc-simple-fenced:
  - $1: "arc"
  - %1 (pause:short)
  - %2 (pause:short)

# Speaks an inverted trigonometric function as arcus. Fallback for complex arguments.
# Example: arc sine of open paren x plus y close paren
# LaTeX Example: $\sin^{-1} (x+y)$
# %1: The inverted function (e.g., sin^-1)
# %2: The fenced argument with simple content
function-prefix-arc:
  - $1: "arc"
  - %1
  - $2: "of"
  - %2 (pause:short)

# General inverse function
# Example: f inverse
# LaTeX Example: $f^{-1}$
# %1: The inverse function
function-inverse:
  - %1
  - $1: "inverse"

# General function with superscript that is either a number of simple identifier.
# %1: The prefix function as basis
# %2: The superscript
superscript-prefix-function:
  - %1 (grammar:addArticle)
  - $1: "power of"
  - %2

# The fallback rule for superscript/exponent expressions
# %1: The basis
# %2: The exponent
superscript:
  - %1
  - $1: "raised to the exponent" (pause:short)
  - %2 (pause:short)
  - $2: "end exponent" (pause:short)

# Rule for exponent expressions with single letter or integer exponents in Ordinal preference.
# Example: x to the y-th
# LaTeX Example: $x^{y}$
# %1: The basis
# %2: The exponent
superscript-simple-exponent:
  - %1
  - $1: "raised to the"
  - %2
  - $2: "power" (pause:short)

# Rule for exponent expressions with simple exponents where power is used as postfix for disambiguation.
# Example: x raised to the 2 y power
# LaTeX Example: $x^{2y}$
# %1: The basis
# %2: The exponent
superscript-simple-exponent-end:
  - %1
  - $1: "raised to the"
  - %2
  - $2: "power"

# Fallback rule for exponent expressions with integer or simple single letter identifier exponent
# %1: The basis
# %2: The exponent as an integer or single letter identifier
superscript-ordinal:
  - %1
  - $1: "to the"
  - %2
  - $2: "power" (pause:short)

# Rule for exponent expressions with simple exponents that should not be read a ordinals in Ordinal preference.
# Example: x to the negative 1
# LaTeX Example: $x^{-1}$
# %1: The basis
# %2: The exponent
superscript-non-ordinal:
  - %1
  - $1: "to the"
  - %2
  - $2: "power" (pause:short)

# 
# %1: 
# %2: 
superscript-simple-function:
  - %1 (grammar:addArticle)
  - $1: "power of" (pause:short)
  - %2

# Simple letter function (e.g., f, g, h) with exponent explicitly not spoken as a function. Usually does not need to be localised.
# %1: The single letter function with exponent
superscript-simple-function-none:
  - %1

# 
# %1: 
# %2: 
superscript-ordinal-number:
  - %1
  - $1: "to the"
  - %2 (pause:short)

# 
# %1: 
# %2: 
superscript-ordinal-negative:
  - %1
  - $1: "to the"
  - %2 (pause:short)

# Fallback rule for exponent expressions in Ordinal preference.
# Example: x raised to the y plus z power
# LaTeX Example: $x^{y+z}$
# %1: The basis
# %2: The exponent
superscript-ordinal-default:
  - %1
  - $1: "raised to the exponent" (pause:short)
  - %2 (pause:short)
  - $2: "end exponent" (pause:short)

# 
# %1: 
# %2: 
superscript-ordinal-power-number:
  - %1
  - $1: "to the"
  - %2
  - $2: "power" (pause:short)

# 
# %1: 
# %2: 
superscript-ordinal-power-negative:
  - %1
  - $1: "to the"
  - %2
  - $2: "power" (pause:short)

# 
# %1: 
# %2: 
superscript-ordinal-power-identifier:
  - %1
  - $1: "to the"
  - %2
  - $2: "power" (pause:short)

# 
# %1: 
# %2: 
superscript-ordinal-power-default:
  - %1
  - $1: "raised to the exponent" (pause:short)
  - %2 (pause:short)
  - $2: "end exponent" (pause:short)

# 
# %1: 
# %2: 
superscript-power:
  - %1
  - $1: "raised to the power"
  - %2 (pause:short)

# 
# %1: 
# %2: 
superscript-power-default:
  - %1
  - $1: "raised to the exponent" (pause:short)
  - %2 (pause:short)
  - $2: "end exponent" (pause:short)

# A letter spoken as ordinal exponent. Note that the rule might need adjustment of the join attribute.
# Example: y-th as in x to the y-th
# LaTeX Example: $y$
# %1: The letter
exponent:
  - %1 (join:"-")
  - $1: "th"

# A number spoken as an ordinal. Note that the rule might need adjustment of the join attribute.
# Example: fourth as in x to the fourth
# LaTeX Example: $4$
# %1: The number
exponent-number:
  - %1

# 
# %1: 
exponent-ordinal:
  - %1

# 
exponent-ordinal-zero:
  - $1: "zero"

# 
# %1: 
exponent-ordinalpower:
  - %1

# 
exponent-ordinalpower-zero:
  - $1: "zero"

# A squared element
# Example: x squared
# LaTeX Example: $x^2$
# %1: The expression that is squared.
square:
  - %1
  - $1: "squared"

# A cubed element
# Example: x cubed
# LaTeX Example: $x^3$
# %1: The expression that is cubed.
cube:
  - %1
  - $1: "cubed"

# Parenthesised expression interpreted as point
# Example: The point with coordinates 5 comma 6
# LaTeX Example: $(5,6)$
# %1: The two comma separated points
fences-points:
  - $1: "the point with coordinates"
  - %1

# A fenced pair of values interpreted as in interval. This is the base rule, specialisations are handled in subsequent rules.
# Example: The interval from a to b
# LaTeX Example: $[a,b]$
# %1: The start value
# %2: The end value
# %3: The overall interval for speaking specialisations, such as inclusions or exclusions of boundaries
fences-interval:
  - $1: "the interval from"
  - %1
  - $2: "to"
  - %2 (pause:short)
  - %3

# Specialisation for an open interval
# LaTeX Example: $(a,b)$
# %1: The start value
# %2: The end value
interval-open:
  - $1: "not including"
  - %1
  - $2: "or"
  - %2

# Specialisation for an closed-open interval
# LaTeX Example: $[a,b)$
# %1: The start value
# %2: The end value
interval-closed-open:
  - $1: "including"
  - %1 (pause:short)
  - $2: "but not including"
  - %2

# Specialisation for an open-closed interval
# LaTeX Example: $(a,b]$
# %1: The start value
# %2: The end value
interval-open-closed:
  - $1: "not including"
  - %1 (pause:short)
  - $2: "but including"
  - %2

# Specialisation for an closed interval
# LaTeX Example: $[a,b]$
# %1: The start value
# %2: The end value
interval-closed:
  - $1: "including"
  - %1
  - $2: "and"
  - %2

# Open integral with right infinity
# LaTeX Example: $(a,\infty)$
# %1: The start value
interval-open-inf-r:
  - $1: "not including"
  - %1

# Open integral with left infinity
# LaTeX Example: $(\infty,b)$
# %1: The end value
interval-open-inf-l:
  - $1: "not including"
  - %1

# Open integral with right infinity
# LaTeX Example: $[a,\infty)$
# %1: The start value
interval-closed-open-inf:
  - $1: "including"
  - %1

# Closed integral with left infinity
# LaTeX Example: $(\infty,b]$
# %1: The end value
interval-open-closed-inf:
  - $1: "including"
  - %1

# The empty set
# Example: the empty set
# LaTeX Example: $\empty$
set-empty:
  - $1: "the empty set"

# A set defined via set intensionality
# Example: The set of all a in A such that ...
# LaTeX Example: $\{a\in A| ...\}$
# %1: The element left of the divider
# %2: The element to the right.
set-extended:
  - $1: "the set of all"
  - %1
  - $2: "such that"
  - %2

# Set defined via a collection of elements
# Example: The set a comma b comma c
# LaTeX Example: $\{a,b,c\}$
# %1: The content of the set
set-collection:
  - $1: "the set"
  - %1

# A set defined via set intensionality without an explicity for all
# Example: The set a in A such that ...
# LaTeX Example: $\{a\in A| ...\}$
# %1: The element left of the divider
# %2: The element to the right.
set-extended-woall:
  - $1: "the set of"
  - %1
  - $2: "such that"
  - %2

# Fallback rule for subscript expressions
# Example: A sub a
# LaTeX Example: $A_a$
# %1: The base
# %2: The subscript index
subscript:
  - %1
  - $1: "sub"
  - %2 (pause:short)

# Logarithm spoken as base
# %1: Logarithm function
# %2: The base
logarithm-base:
  - %1
  - $1: "base"
  - %2

# An element with an index
# %1: The base element
# %2: The index
subscript-index:
  - %1
  - $1: "sub"
  - %2

# Fallback rule for explicit speaking of fraction
# Example: the fraction with numerator a and denominator b
# LaTeX Example: $\frac{a}{b}$
# %1: The numerator
# %2: The denominator
fraction:
  - $1: "the" (grammar:article)
  - $2: "fraction with numerator"
  - %1 (pause:short)
  - $3: "and denominator"
  - %2 (pause:short)

# A fraction in a function application, which is not interpreted as a function
# LaTeX Example: $\frac{f}{g}(x)$
# %1: The numerator
# %2: The denominator
fraction-none:
  - $1: "the" (grammar:article)
  - $2: "fraction with numerator"
  - %1 (pause:short)
  - $3: "and denominator"
  - %2 (pause:short)

# Fraction with simple elements in numerator and denominator
# Example: a over b
# LaTeX Example: $\frac{a}{b}$
# %1: The numerator
# %2: The denominator
simple-fraction:
  - %1
  - $1: "over"
  - %2 (pause:short)

# Vulgar fraction with positive integer numerator > 10 or denominator > 20. Not spoken as words
# %1: The numerator
# %2: The denominator
simple-vulgar-fraction:
  - %1
  - $1: "over"
  - %2 (pause:short)

# Fraction with simple textual elements
# Example: containers over hours
# %1: The numerator
# %2: The denominator
simple-text-fraction:
  - %1
  - $1: "over"
  - %2 (pause:short)

# Vulgar fraction spoken as words. The rule fires for positive integer numerators <= 10 and denominators <=20
# %1: The vulgar fraction. Add pausing or grammar instructions if necessary.
vulgar-fraction:
  - %1

# Short speech for fractions with simple components.
# Example: a over b
# LaTeX Example: $\frac{a}{b}$
# %1: The numerator
# %2: The denominator
fraction-over:
  - %1
  - $1: "over"
  - %2 (pause:short)

# Short speech for fractions with simple components with explicit end fraction
# Example: a over b end fraction
# LaTeX Example: $\frac{a}{b}$
# %1: The numerator
# %2: The denominator
fraction-overendfrac:
  - %1
  - $1: "over"
  - %2 (pause:short)
  - $2: "end fraction" (pause:short)

# Short speech for fractions with simple components with explicitly announced fraction
# Example: the fraction a over b
# LaTeX Example: $\frac{a}{b}$
# %1: The numerator
# %2: The denominator
fraction-fracover:
  - $1: "the fraction"
  - %1
  - $2: "over"
  - %2 (pause:short)

# Short speech for fractions with simple components using per
# Example: a per b
# LaTeX Example: $\frac{a}{b}$
# %1: The numerator
# %2: The denominator
fraction-per:
  - %1
  - $1: "per"
  - %2 (pause:short)

# Fallback rule for all fractions with explicit end fraction announcement. This is for the generalEndFrac preference, where all fractions are announced the same followed by end fraction.
# Example: the fraction with numerator a plus b and denominator c plus d end fraction
# LaTeX Example: $\frac{a+b}{c+d}$
# %1: The numerator
# %2: The denominator
fraction-generalendfrac:
  - $1: "the fraction with numerator"
  - %1 (pause:short)
  - $2: "and denominator"
  - %2 (pause:short)
  - $3: "end fraction" (pause:short)

# Fallback rule for all fractions. This is for the general preference, where all fractions are announced the same.
# Example: the fraction with numerator a and denominator c
# LaTeX Example: $\frac{a}{c}$
# %1: 
# %2: 
fraction-general:
  - $1: "the fraction with numerator"
  - %1 (pause:short)
  - $2: "and denominator"
  - %2 (pause:short)

# A vulgar fraction pronounced as ordinal in all cases.
# %1: The vulgar fraction
simple-vulgar-fraction-ordinal:
  - %1

# The rule adds end fraction announcements to fractions if necessary. Used in EndFrac preference.
# %1: The fraction
fraction-endfrac:
  - %1
  - $1: "end fraction" (pause:short)

# Vulgar fraction with positive integer numerator > 10 or denominator > 20. Not spoken as words in EndFrac preference.
# %1: The numerator
# %2: The denominator
vulgar-fraction-endfrac:
  - %1
  - $1: "over"
  - %2 (pause:short)

# Vulgar fraction with positive integer numerator <= 10 or denominator <= 20, spoken as words in EndFrac preference.
# %1: The vulgar fraction
simple-vulgar-fraction-endfrac:
  - %1

# Basic square root rule
# %1: Content of the square root
sqrt:
  - $1: "the square root of"
  - %1 (pause:short)

# A square root, nested within another. This is never spoken with an `end root` announcement
# %1: The content of the square root.
sqrt-nested:
  - $1: "the square root of"
  - %1 (pause:short)

# A negative square root
# %1: The content of the square root.
negative-sqrt:
  - $1: "the negative square root of"
  - %1 (pause:short)

# A negative square root, possibly nested within another. This is never spoken with an `end root` announcement
# %1: The content of the square root.
negative-sqrt-default:
  - $1: "the negative square root of"
  - %1 (pause:short)

# Square root with plus-minus sign spoken as positive square root in PosNegSqRoot preference
# Example: plus or minus the positive square root of a
# LaTeX Example: $\pm \sqrt{a}$
# %1: The content of the square root.
sqrt-plus-minus:
  - $1: "the positive square root of"
  - %1 (pause:short)

# Nested square root with plus-minus sign spoken as positive square root in PosNegSqRoot preference. This is never spoken with an `end root` announcement
# Example: plus or minus the positive square root of a
# LaTeX Example: $\pm \sqrt{a}$
# %1: The content of the square root.
sqrt-nested-plus-minus:
  - $1: "the positive square root of"
  - %1 (pause:short)

# Adds a end root announcement if necessary.
# %1: The square root.
sqrt-plus-minus-posnegsqrootend:
  - $1: "the positive square root of"
  - %1 (pause:short)

# Adds a end root announcement if necessary.
# %1: The square root.
sqrt-nested-plus-minus-posnegsqrootend:
  - $1: "the positive square root of"
  - %1 (pause:short)

# Adds a end root announcement if necessary.
# %1: The square root.
sqrt-endroot:
  - %1
  - $1: "end root" (pause:short)

# Adds a end root announcement if necessary.
# %1: The negative square root
negative-sqrt-endroot:
  - %1
  - $1: "end root" (pause:short)

# Adds a end root announcement if necessary.
# %1: The square root
sqrt-posnegsqrootend:
  - %1
  - $1: "end root" (pause:short)

# Adds a end root announcement if necessary.
# %1: The negative square root
negative-sqrt-posnegsqrootend:
  - %1
  - $1: "end root" (pause:short)

# Basic rule for cubic roots
# %1: The content of the cubic root
cubic:
  - $1: "the cube root of"
  - %1 (pause:short)

# A cubic root, nested within another. This is never spoken with an `end root` announcement
# %1: The content of the cubic root
cubic-nested:
  - $1: "the cube root of"
  - %1 (pause:short)

# An root with explicit index. Speaking the index as ordinal when possible.
# Example: the fourth root of a
# LaTeX Example: $\sqrt[4]{a}$
# %1: Index of the root
# %2: Content of the radical
root:
  - %1 (grammar:addArticle)
  - $1: "root of"
  - %2 (pause:short)

# An nested root with explicit index. Never spoken with `end root` announcement.
# %1: Index of the root
# %2: Content of the radical
root-nested:
  - %1 (grammar:addArticle)
  - $1: "root of"
  - %2 (pause:short)

# Adds a end root announcement if necessary.
# %1: The root
root-endroot:
  - %1
  - $1: "end root" (pause:short)

# Adds a end root announcement if necessary. Specialist rule for PosNegSqRoot
# %1: The root
root-posnegsqrootend:
  - %1
  - $1: "end root" (pause:short)

# Pronounces the minus sign, when used as prefix sign rather than an operation
# Example: negative a
# LaTeX Example: $-a$
# %1: The element with the negative sign
negative:
  - $1: "negative"
  - %1

# Pronounces the plus sign, when used as prefix sign rather than an operation
# Example: positive a
# LaTeX Example: $+a$
# %1: The element with the positive sign
positive:
  - $1: "positive"
  - %1

# 
# %1: 
# %2: 
angle-measure:
  - $1: "the measure of"
  - %1
  - %2

# 
# %1: 
set-prefix-operators:
  - %1 (grammar:addArticle)
  - $1: "of"

# An inline division with forward slash or division sign
# Example: a divided by b
# LaTeX Example: $a/b$
# %1: 
# %2: 
division:
  - %1
  - $1: "divided by"
  - %2

# 
# %1: 
operators-after-power:
  - %1

# The natural numbers (positive integers)
natural-numbers:
  - $1: "the natural numbers"

# The integers
integers:
  - $1: "the integers"

# The rational numbers
rational-numbers:
  - $1: "the rational numbers"

# The real numbers
real-numbers:
  - $1: "the real numbers"

# The complex numbers
complex-numbers:
  - $1: "the complex numbers"

# The Natural number with zero
# LaTeX Example: $\N_0$
natural-numbers-with-zero:
  - $1: "the natural numbers with zero"

# The positive integers
# LaTeX Example: $\Z^{+}$
positive-integers:
  - $1: "the positive integers"

# The negative integers
# LaTeX Example: $\Z^{-}$
negative-integers:
  - $1: "the negative integers"

# The positive rational numbers
# LaTeX Example: $\Q^{+}$
positive-rational-numbers:
  - $1: "the positive rational numbers"

# The negative rational numbers
# LaTeX Example: $\Q^{-}$
negative-rational-numbers:
  - $1: "the negative rational numbers"

# Absolute value
# %1: The content of the absolute value
fences-neutral:
  - $1: "the absolute value of"
  - %1 (pause:short)

# Absolute values spoken with explicit end of absolute value
# %1: The content of the absolute value
fences-neutral-absend:
  - $1: "the absolute value of"
  - %1 (pause:short)
  - $2: "end absolute value" (pause:short)

# Neutral interpreted as cardinality of a set
# Example: cardinality of S
# LaTeX Example: $|S|$
# %1: The content between the neutral fences
fences-neutral-cardinality:
  - $1: "the cardinality of"
  - %1 (pause:short)

# Neutral interpreted as determinant of a matrix
# Example: determinant of A
# LaTeX Example: $|A|$
# %1: The content between the neutral fences
fences-neutral-determinant:
  - $1: "the determinant of"
  - %1 (pause:short)

# 
# %1: 
fences-metric:
  - $1: "the metric of"
  - %1 (pause:short)

# 
# %1: 
fences-metric-absend:
  - $1: "the metric of"
  - %1 (pause:short)
  - $2: "end metric" (pause:short)

# 
# %1: 
# %2: 
# %3: 
matrix:
  - $1: "the"
  - %1
  - $2: "by"
  - %2
  - $3: "matrix" (pause:long)
  - %3 (context:"Row-:", pause:long)

# 
# %1: 
# %2: 
# %3: 
matrix-simple:
  - $1: "the"
  - %1
  - $2: "by"
  - %2
  - $3: "matrix" (pause:long)
  - %3 (context:"Row-:", pause:long)

# 
# %1: 
matrix-trivial:
  - $1: "the 1 by 1 matrix with entry"
  - %1 (pause:long)

# 
# %1: 
# %2: 
# %3: 
determinant:
  - $1: "the determinant of the"
  - %1
  - $2: "by"
  - %2
  - $3: "matrix" (pause:long)
  - %3 (context:"Row-:", pause:long)

# 
# %1: 
# %2: 
# %3: 
determinant-simple:
  - $1: "the determinant of the"
  - %1
  - $2: "by"
  - %2
  - $3: "matrix" (pause:long)
  - %3 (context:"Row-:", pause:long)

# 
# %1: 
# %2: 
# %3: 
matrix-vector:
  - $1: "the"
  - %1
  - $2: "by"
  - %2
  - $3: "column matrix" (pause:long)
  - %3 (context:"Row-:", pause:long)

# 
# %1: 
# %2: 
# %3: 
matrix-vector-simple:
  - $1: "the"
  - %1
  - $2: "by"
  - %2
  - $3: "column matrix" (pause:long)
  - %3 (pause:long)

# 
# %1: 
# %2: 
# %3: 
matrix-vector-simple-silentcolnum:
  - $1: "the"
  - %1
  - $2: "by"
  - %2
  - $3: "column matrix" (pause:long)
  - %3 (pause:long)

# 
# %1: 
# %2: 
# %3: 
matrix-row-vector:
  - $1: "the"
  - %1
  - $2: "by"
  - %2
  - $3: "row matrix" (pause:long)
  - %3 (context:"Column-:", pause:long)

# 
# %1: 
# %2: 
# %3: 
matrix-row-vector-simple:
  - $1: "the"
  - %1
  - $2: "by"
  - %2
  - $3: "row matrix" (pause:long)
  - %3 (pause:long)

# 
# %1: 
# %2: 
# %3: 
matrix-row-vector-simple-silentcolnum:
  - $1: "the"
  - %1
  - $2: "by"
  - %2
  - $3: "row matrix" (pause:long)
  - %3 (pause:long)

# 
# %1: 
matrix-row:
  - %1 (context:"Column-,- ", pause:long)

# 
# %1: 
matrix-end-matrix:
  - %1
  - $1: "end matrix"

# 
# %1: 
matrix-end-vector:
  - %1
  - $1: "end matrix"

# 
# %1: 
matrix-end-determinant:
  - %1
  - $1: "end determinant"

# 
# %1: 
# %2: 
# %3: 
vector:
  - $1: "the"
  - %1
  - $2: "by"
  - %2
  - $3: "column vector" (pause:long)
  - %3 (context:"Row-:", pause:long)

# 
# %1: 
# %2: 
# %3: 
vector-simple:
  - $1: "the"
  - %1
  - $2: "by"
  - %2
  - $3: "column vector" (pause:long)
  - %3 (pause:long)

# 
# %1: 
# %2: 
# %3: 
row-vector:
  - $1: "the"
  - %1
  - $2: "by"
  - %2
  - $3: "row vector" (pause:long)
  - %3 (context:"Column-:", pause:long)

# 
# %1: 
# %2: 
# %3: 
row-vector-simple:
  - $1: "the"
  - %1
  - $2: "by"
  - %2
  - $3: "row vector" (pause:long)
  - %3 (pause:long)

# 
# %1: 
vector-end-matrix:
  - %1
  - $1: "end matrix"

# 
# %1: 
vector-end-vector:
  - %1
  - $1: "end vector"

# 
# %1: 
vector-end-vector-endvector:
  - %1
  - $1: "end vector"

# 
# %1: 
vector-end-determinant:
  - %1
  - $1: "end determinant"

# 
# %1: 
# %2: 
binomial:
  - %1
  - $1: "choose"
  - %2

# 
# %1: 
# %2: 
lines-summary:
  - %1
  - $1: "lines"
  - %2

# 
# %1: 
# %2: 
cases-summary:
  - %1
  - $1: "cases"
  - %2

# 
# %1: 
lines:
  - %1 (context:"Line-:", pause:long)

# 
blank-cell:
  - $1: "blank"

# 
blank-line:
  - $1: "blank"

# 
blank-cell-empty:
  - $1: "blank"

# 
blank-line-empty:
  - $1: "blank"

# 
# %1: 
cases:
  - %1 (context:"Case-:", pause:long)

# 
# %1: 
# %2: 
lines-cases-summary:
  - %1
  - $1: "cases"
  - %2

# 
# %1: 
lines-cases:
  - %1 (context:"Case-:", pause:long)

# 
# %1: 
# %2: 
lines-equations-summary:
  - %1
  - $1: "equations"
  - %2

# 
# %1: 
lines-equations:
  - %1 (context:"Equation-:", pause:long)

# 
# %1: 
# %2: 
lines-steps-summary:
  - %1
  - $1: "steps"
  - %2

# 
# %1: 
lines-steps:
  - %1 (context:"Step-:", pause:long)

# 
# %1: 
# %2: 
lines-rows-summary:
  - %1
  - $1: "rows"
  - %2

# 
# %1: 
lines-rows:
  - %1 (context:"Row-:", pause:long)

# 
# %1: 
# %2: 
lines-constraints-summary:
  - %1
  - $1: "constraints"
  - %2

# 
# %1: 
lines-constraints:
  - %1 (context:"Constraint-:", pause:long)

# 
# %1: 
# %2: 
bigop:
  - %1 (grammar:addArticle)
  - $1: "of"
  - %2 (pause:short)

# 
# %1: 
# %2: 
# %3: 
limboth:
  - %1
  - $1: "from"
  - %2
  - $2: "to"
  - %3

# 
# %1: 
# %2: 
limlower:
  - %1
  - $1: "over"
  - %2 (pause:short)

# 
# %1: 
# %2: 
limupper:
  - %1
  - $1: "under"
  - %2 (pause:short)

# 
# %1: 
# %2: 
integral:
  - %1 (grammar:addArticle)
  - $1: "of"
  - %2 (pause:short)

# 
# %1: 
# %2: 
overscript:
  - %1
  - $1: "under"
  - %2 (pause:short)

# 
# %1: 
# %2: 
overscript-limits:
  - %1
  - $1: "to"
  - %2

# 
# %1: 
# %2: 
underscript:
  - %1
  - $1: "over"
  - %2 (pause:short)

# 
# %1: 
# %2: 
underscript-limits:
  - %1
  - $1: "from"
  - %2

# Mixed numbers
# Example: Two and one third
# LaTeX Example: $2 \frac{1}{3}$
# %1: The integer part
# %2: The fraction part
mixed-number:
  - %1
  - $1: "and"
  - %2

# A number that contains letters
# Example: Number #A0FF
# %1: The number
number-with-chars:
  - $1: "number"
  - %1

# 
# %1: 
# %2: 
decimal-period:
  - $1: "the repeating decimal"
  - %1
  - $2: "point followed by repeating digits"
  - %2

# 
# %1: 
# %2: 
decimal-period-float:
  - $1: "the repeating decimal"
  - %1
  - $2: "followed by repeating digits"
  - %2

# 
# %1: 
# %2: 
decimal-period-singular:
  - $1: "the repeating decimal"
  - %1
  - $2: "point followed by repeating digit"
  - %2

# 
# %1: 
# %2: 
decimal-period-singular-float:
  - $1: "the repeating decimal"
  - %1
  - $2: "followed by repeating digit"
  - %2

# 
decimal-point:
  - $1: "point"

# 
# %1: 
# %2: 
line-segment:
  - $1: "the line segment"
  - %1
  - %2 (pause:short)

# 
# %1: 
conjugate:
  - $1: "the complex conjugate of"
  - %1

# 
defined-by:
  - $1: "is defined to be" (pause:short)

# 
# %1: 
# %2: 
adorned-sign:
  - %1
  - $1: "sign with"
  - %2
  - $2: "over it"

# 
factorial:
  - $1: "factorial"

# 
# %1: 
left-super:
  - $1: "left super"
  - %1

# 
# %1: 
left-super-list:
  - $1: "left super"
  - %1

# 
# %1: 
left-sub:
  - $1: "left sub"
  - %1

# 
# %1: 
left-sub-list:
  - $1: "left sub"
  - %1

# 
# %1: 
right-super:
  - $1: "right super"
  - %1

# 
# %1: 
right-super-list:
  - $1: "right super"
  - %1

# 
# %1: 
right-sub:
  - $1: "right sub"
  - %1

# 
# %1: 
right-sub-list:
  - $1: "right sub"
  - %1

# 
# %1: 
# %2: 
choose:
  - %1
  - $1: "choose"
  - %2

# 
# %1: 
# %2: 
permute:
  - %1
  - $1: "permute"
  - %2

# A unit that is squared
# %1: The unit
unit-square:
  - $1: "square"
  - %1

# A unit that is cubed
# %1: The unit
unit-cubic:
  - $1: "cubic"
  - %1

# A unit to the power of negative one
# %1: The unit
unit-reciprocal:
  - $1: "reciprocal"
  - %1

# 
# %1: 
unit-reciprocal-singular:
  - $1: "per"
  - %1 (grammar:singular)

# A fraction of units
# %1: Unit in enumerator
# %2: Unit in denominator
unit-divide:
  - %1
  - $1: "per"
  - %2 (grammar:singular)

# 
# %1: 
# %2: 
enclose:
  - $1: "enclosed with"
  - %1
  - %2

# 
# %1: 
# %2: 
enclose-end:
  - $1: "enclosed with"
  - %1
  - %2
  - $2: "end enclosed"

# 
# %1: 
overbar:
  - %1
  - $1: "horizontal bar"

# 
# %1: 
underbar:
  - %1
  - $1: "over horizontal bar"

# 
# %1: 
leftbar:
  - $1: "vertical bar"
  - %1

# 
# %1: 
rightbar:
  - %1
  - $1: "vertical bar"

# 
# %1: 
crossout:
  - $1: "crossed out"
  - %1

# 
# %1: 
crossout-end:
  - $1: "crossed out"
  - %1
  - $2: "end crossout"

# 
# %1: 
# %2: 
cancel-over:
  - %1
  - $1: "crossed out with"
  - %2

# 
# %1: 
# %2: 
cancel-under:
  - %1
  - $1: "crossed out with"
  - %2

# 
# %1: 
# %2: 
cancel-over-end:
  - $1: "crossed out"
  - %1
  - $2: "with"
  - %2
  - $3: "end crossout"

# 
# %1: 
# %2: 
cancel-under-end:
  - $1: "crossed out"
  - %1
  - $2: "with"
  - %2
  - $3: "end crossout"
